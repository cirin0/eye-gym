import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.cirin0.eyegym',
  appName: 'Eye Gym',
  webDir: 'dist',
  backgroundColor: '#121212',
  android: {
    backgroundColor: '#121212',
  },
}

export default config
